# Testcontainers configuration
# Fixes Docker API version compatibility issue with Docker Desktop / Docker Engine 26+
# (where API versions < 1.40 are deprecated and rejected with HTTP 400)
api.version=1.44
